<launch>
    <arg name="use_gazebo" default="false"/>

    <!-- TODO may need to make address resolution more robust -->
    <!-- http://wiki.ros.org/ROS/NetworkSetup -->

    <!-- local slave - control station -->
    <group>
        <machine name="dev-machine"
            address="$(env ROS_HOSTNAME)"
            env-loader="/opt/ros/$(env ROS_DISTRO)/env.sh"/>

        <node pkg="joy"
            name="joy_node"
            type="joy_node"
            output="screen">
            <param name="~dev" value="/dev/input/js0"/>
        </node>

        <node pkg="teleop_twist_joy"
            name="teleop_node"
            type="teleop_node"
            output="screen">
            <!-- mappings for x360 controller -->
            <param name="~enable_button" value="4"/>
            <param name="~enable_turbo_button" value="5"/>
            <param name="~axis_linear" value="1" type="int"/>
            <param name="~axis_angular" value="3" type="int"/>

            <!-- TODO map to base_controller speed params -->
            <param name="~scale_linear" value="0.3" type="double"/>
            <param name="~scale_linear_turbo" value="0.5" type="double"/>

            <remap from="cmd_vel" to="/base_controller/cmd_vel"/>
        </node>

        <node name="rviz" pkg="rviz" type="rviz" args="-d $(find robot_control)/rviz/teleop.rviz"/>

        <include file="$(find robot_base)/launch/base.launch" if="$(arg use_gazebo)">
            <arg name="use_gazebo" value="true"/>
            <arg name="joystick_available" value="true"/>
        </include>
    </group>

    <group unless="$(arg use_gazebo)">
        <machine name="robot"
            address="robot-desktop"
            env-loader="/opt/ros/melodic/env.sh"
            user="robot"/>

        <include file="$(find robot_base)/launch/base.launch">
            <arg name="use_gazebo" value="false"/>
            <arg name="joystick_available" value="true"/>
        </include>
    </group>
</launch>
